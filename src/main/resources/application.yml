spring:
  application:
    name: Liga_CUT

  datasource:
    url: jdbc:mysql://localhost:3306/ligacut?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update    # en desarrollo 'update'. En producción usa 'validate'
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8080

logging:
  level:
    com.proyecto.ligacut: DEBUG